## Agent skills

### Issue tracker

Issues and PRDs are tracked on GitHub. See `docs/agents/issue-tracker.md`.

### Triage labels

Using standard triage labels (`needs-triage`, `ready-for-agent`, etc.). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context layout with `CONTEXT.md` and `docs/adr/` at the root. See `docs/agents/domain.md`.
